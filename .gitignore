node_modules/
recordings/
data.json
data/
*.log
.DS_Store
config.json
.env
*.pem
SornigReadMe.txt
.privacy-mode
.hidden-cams/
# atomic-write temp files (e.g. .data.json.<pid>.tmp) — may contain secrets
.*.tmp
# data.json backups — contain password hashes + session tokens, never commit
backups/
